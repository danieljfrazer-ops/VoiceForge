/* ============ VoiceForge frontend config ============ */
/* Overridable per-deployment (e.g. Cloudflare Pages build step can rewrite this file). */
window.VOICEFORGE_CONFIG = {
  mode: "auto",            // "auto" | "demo" | "local"
  repoUrl: "https://github.com/danieljfrazer-ops/VoiceCloner",
};
